# 🌸 AIgul — AI-флорист, который понимает мужчин

Веб-приложение цветочного магазина с AI-агентом на базе **Google Gemini**.
Помогает мужчинам выбирать идеальные букеты для их женщин — задаёт уточняющие
вопросы, понимает контекст (ссора, годовщина, бюджет) и даёт **персонализированные**
рекомендации, а не просто кидает каталог.

---

## ✨ Что это решает

> Мужчины не умеют выбирать цветы. Они либо берут банальные красные розы,
> либо ходят 40 минут по магазину растерянные. Айгуль решает это за 2 минуты.

**Главный сценарий (демо для жюри):**
> *Клиент:* «Блин, мы жёстко поссорились, мне нужны цветы, чтобы извиниться, бюджет 10к тенге»
>
> *Айгуль:* не предлагает банальные красные розы. Понимает, что это извинения и нужен жест примирения, а не страсти. Рекомендует **белые пионы (символ примирения и чистых намерений)** — 7 шт за 9 500 ₸. Объясняет почему именно это, предлагает добавить открытку, помогает оформить.

---

## 🧠 Технологии

| Слой | Стек |
|---|---|
| Frontend | Next.js 14 (App Router), React 18, TypeScript |
| UI | Tailwind CSS, Framer Motion, Lucide Icons |
| AI | Google Gemini 2.0 Flash (`@google/generative-ai`) |
| RAG | Структурированная база знаний магазина (каталог + правила + гайд по поводам), инжектится в `systemInstruction` |
| Backend | Next.js API Routes (`/api/chat`, `/api/order`) |

---

## 🚀 Быстрый старт

### 1. Установка

```bash
npm install
```

### 2. Получите API-ключ Gemini

Бесплатный ключ: <https://aistudio.google.com/app/apikey>

### 3. Создайте `.env.local`

```bash
cp .env.example .env.local
```

И вставьте ключ:

```
GEMINI_API_KEY=your_api_key_here
```

### 4. Запустите dev-сервер

```bash
npm run dev
```

Откройте <http://localhost:3000>

---

## 🏗️ Структура проекта

```
src/
├── app/
│   ├── layout.tsx              # Глобальный layout (шрифты, метаданные)
│   ├── page.tsx                # Главная: Hero + Chat + How it works
│   ├── globals.css             # Tailwind + кастомные анимации
│   ├── catalog/page.tsx        # Каталог цветов с фильтрами
│   ├── checkout/page.tsx       # Оформление заказа (с автозаполнением из чата)
│   └── api/
│       ├── chat/route.ts       # Gemini chat endpoint (с RAG-контекстом)
│       └── order/route.ts      # Приём заказов
├── components/
│   ├── Header.tsx              # Шапка с навигацией
│   ├── Hero.tsx                # Hero-секция с анимациями
│   ├── ChatInterface.tsx       # ⭐ Главный компонент — чат с AI-агентом
│   ├── HowItWorks.tsx          # Секция «как это работает»
│   └── Footer.tsx              # Подвал
└── lib/
    ├── knowledge-base.ts       # 🧠 База знаний магазина (RAG-данные)
    └── system-prompt.ts        # 🎯 Системный промпт для Gemini
```

---

## 🎯 Как работает AI-агент

1. **Системный промпт** (`src/lib/system-prompt.ts`) задаёт Gemini роль премиум-флориста-«вингмана»: эмпатия, юмор, вопросы по одному, никаких 10-вопросных анкет.
2. **RAG** (`src/lib/knowledge-base.ts`) — структурированный каталог магазина (цены, значения цветов, гайд по поводам, правила этикета) инжектится прямо в `systemInstruction`. В реальном проде здесь подключается векторная БД (Pinecone / pgvector), но для MVP-демо хватает.
3. **Чат** (`/api/chat`) — Next.js API Route шлёт историю в `gemini-2.0-flash-exp` через `startChat({ history })`.
4. **Передача контекста в checkout** — после рекомендации UI сохраняет историю чата в `localStorage`, страница оформления автоматически подтягивает её и подставляет в карточку «Рекомендация Айгуль».

---

## 🛡️ Демо-сценарии (для защиты)

Кнопки готовых сценариев уже встроены в чат:

- 🔥 «Блин, мы жёстко поссорились…» — проверяет понимание контекста + бюджета
- 💝 «Годовщина 2 года, хочу удивить» — проверяет рекомендацию по сроку отношений
- 🌷 «Первое свидание, не хочу спугнуть» — проверяет анти-паттерн (нет красных роз)
- 👩 «Маме на день рождения, ей 55» — проверяет рекомендацию по возрасту

---

## 🎨 Дизайн

- **Палитра:** кремовый фон, пыльно-розовый, шалфейный зелёный, золото
- **Шрифты:** Playfair Display (заголовки) + Inter (текст)
- **Анимации:** Framer Motion + custom keyframes (плавающие градиенты, fade-up, typing-индикатор)
- **Glassmorphism:** полупрозрачные карточки с backdrop-blur

---

## 📦 Деплой

Самый простой способ — Vercel (нативно поддерживает Next.js):

```bash
npx vercel
```

Не забудьте добавить `GEMINI_API_KEY` в Environment Variables проекта на Vercel.

---

## 🛠️ Что можно улучшить дальше

- [ ] Заменить in-memory знания на векторный поиск (pgvector / Vercel AI SDK Embeddings)
- [ ] Подключить реальную интеграцию с Kaspi Pay
- [ ] Добавить streaming ответов (Server-Sent Events) для эффекта печати
- [ ] WhatsApp-бот для менеджера (отправка заказа после оформления)
- [ ] Tool-calling: Gemini сам формирует JSON-заказ и подставляет в форму

---

Сделано с любовью в Алматы 🌸
